name: CI

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]

jobs:
    test:
        name: Lint and Test
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Go
              uses: actions/setup-go@v5
              with:
                  go-version-file: 'go.mod' # go.modのバージョン(1.26.1)を自動使用
                  cache: true

            - name: Run golangci-lint
              uses: golangci/golangci-lint-action@v6
              with:
                  version: v2.11.4

            - name: Run govulncheck
              run: go run golang.org/x/vuln/cmd/govulncheck@latest ./...

            - name: Run Tests
              run: go test -v -cover ./...
